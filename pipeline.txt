pipeline {
    agent any  // runs on any available agent (node)

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example: compile Java code, npm install, etc.
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Example: run JUnit, pytest, npm test, etc.
            }
        }

        stage('Package') {
            steps {
                echo 'Packaging application...'
                // Example: create JAR, ZIP, Docker image, etc.
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to server...'
                // Example: scp, rsync, kubectl apply, etc.
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully! 🎉'
        }
        failure {
            echo 'Pipeline failed 💥'
        }
    }
}